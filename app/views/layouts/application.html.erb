<!DOCTYPE html>
<html>
  <head>
    <title>JetMaintain</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      body {
        background-image: url('<%= asset_path 'wallpaper.jpg' %>');
        background-size: cover;
        background-repeat: no-repeat;
        background-attachment: fixed;
      }
      .scrollbar {
  margin-left: 30px;
  float: left;
  height: 300px;
  width: 65px;
  background: #fff;
  overflow-y: scroll;
  margin-bottom: 25px;
}
.force-overflow {
  min-height: 450px;
}

.scrollable-card::-webkit-scrollbar {
  width: 12px;
  background-color: #F5F5F5;
}

.scrollable-card::-webkit-scrollbar-thumb {
  border-radius: 10px;
  -webkit-box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.1);
  background-color: #4285F4;
}

.scrollable-card {
  scrollbar-color: #4285F4 #F5F5F5;
}

       .glass {

    background: rgba(8, 0, 255, 0.43);
    border-radius: 16px;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    border: 1px solid rgba(8, 0, 255, 0.3);
      }
    </style>
  </head>

  <body>
    <%= render 'shared/navbar' %>

    <div class="container">
      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.27/dist/sweetalert2.all.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.27/dist/sweetalert2.min.css" rel="stylesheet">

    <!-- Script para mostrar una alerta de ejemplo -->
    <script>
  document.addEventListener('DOMContentLoaded', function() {
    <% if flash[:error] %>
      Swal.fire({
        title: 'Error',
        text: '<%= flash[:error] %>',
        icon: 'error',
        confirmButtonText: 'Aceptar'
      });
    <% end %>
  });
</script>
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Mostrar una alerta al cargar la página
        Swal.fire({
          title: '¡Bienvenido a JetMaintain!',
          text: 'Esta es tu plataforma de mantenimiento de motores y aeronaves.',
          icon: 'info',
          confirmButtonText: 'Aceptar'
        });
      });
    </script>

    <script src="https://kit.fontawesome.com/19e2a818fa.js" crossorigin="anonymous"></script>
  </body>
</html>
